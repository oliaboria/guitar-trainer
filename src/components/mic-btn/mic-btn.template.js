const template = document.createElement('template');

template.innerHTML = `
    <style>
        .red {
            color: red;
        }
    </style>
    <wired-icon-button>
        <mwc-icon>mic_off</mwc-icon>
    </wired-icon-button>
`;

export default template;
